import React from "react";
import { Box, Typography } from "@material-ui/core";


const Home = () => {
    return (
        <Box >
            < Typography style = {{ textAlign: "center" }} variant = "h5" >Welcome to our competition World < /Typography>
        </Box >
    );
};

export default Home;
